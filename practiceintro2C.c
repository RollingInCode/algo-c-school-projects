#include <stdio.h>
#include <stdlib.h>

int main (void) {
    int x1=0,x2= 0;
    int y1,y2;
    int m;
    scanf("(%d %d) / (%d %d)", &x1, &y1, &x2, &y2);
    printf("\n (%d,%d) AND (%d,%d", x1, y1, x2, y2);

    return 0;

}

