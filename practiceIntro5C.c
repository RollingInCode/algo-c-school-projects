#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>

int main (void) {
    int jellyBeans, randomPick;
    double radius, height, radiusJellyBean; //radius of a Jelly Bean is like the volume of a cube

    printf("Height of the cylinder?\n");
    scanf("%d", &height);
    printf("Radius of the cylinder?\n");
    scanf("%d", &radius);

    randomPick = rand((jellyBeans^3) % (M_PI*height*(radius^2));
    printf("The random pick is: %d", randomPick);



    return 0;
}
